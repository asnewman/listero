# Postgres connection string (used by the app and by `npm run db:push`)
DATABASE_URL=postgres://user:password@host:5432/dbname

# Clerk (written by `clerk init`)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
